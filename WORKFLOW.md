# ToB Skills 开发流程

> 更新日期：2026-05-16（v2）

---

## 角色分工

| 角色 | 职责 | 人员 |
|------|------|------|
| **产品/需求** | 提需求、审方案、验收成果 | 李宁 |
| **技能开发** | 代码实现、测试、文档、发布3渠道 | Tech |
| **质量审查** | 代码审查、安全检查、数据一致性、**平台兼容性、隐私安全** | Checker |
| **内容创作** | 写文章、运营素材 | 矛盾（独立创作） |
| **跟进协调** | 更新计划、提醒、同步信息、跨Agent编排 | 二蛋 |

---

## 流程图

### 技能开发流程

```
李宁提需求
    ↓
二蛋记录到 team-tasks + 多维表格
    ↓
派 Tech 开发 + 测试
    ↓
派 Checker 审查（完整审查清单见下方 §Checker 审查清单）
    ↓
Tech 发布到 ClawHub + SkillHub + GitHub
    ↓
【推送前检查清单】（详见 §推送前检查清单）
    ↓
通知二蛋验证确认
    ↓
二蛋更新状态「已完成」
    ↓
（可选）二蛋把技能信息发给矛盾 → 矛盾写文章 → 放入素材库
    ↓
李宁审核/发布
```

### 内容创作流程（矛盾独立）

```
矛盾自主选题创作
    ↓
完成后放入「公众号素材库」
    ↓
状态设为「待审核」
    ↓
李宁审核/发布
```

**技能联动创作（按需触发）：**
```
技能开发完成
    ↓
二蛋把 SKILL.md + 核心功能发给矛盾
    ↓
矛盾决定是否写（不强制）
    ↓
如写，放入素材库
```

---

## 多维表格

### 1. ToB Skills 开发计划
**链接**: https://my.feishu.cn/base/FmXAb41lIa629YsdsdycvNGenUe

| 字段 | 说明 |
|------|------|
| 技能名称 | 英文标识，如 tob-sales-proposal |
| 所属阶段 | Week 1-4 |
| 状态 | 待开始/开发中/已完成/暂停 |
| 预计耗时 | 开发天数 |
| 核心功能 | 一句话描述 |
| GitHub链接 | 代码仓库地址 |

### 2. 公众号素材库
**链接**: https://my.feishu.cn/base/XMyTb8kDOaEubNskK7ecy0jrnKz

| 字段 | 说明 |
|------|------|
| 标题 | 文章标题 |
| 状态 | 草稿/待审核/已发布/搁置 |
| 作者 | 矛盾/李宁/其他 |
| 标签 | 多选，如 ToB销售、AI工具 |
| 正文 | 完整文章内容 |
| 关联技能 | 可选，关联的技能名称 |

---

## § Checker 审查清单（2026-05-16 新增平台兼容性 + 隐私安全）

所有代码/配置/文档变更，Tech 完成后 → 派 Checker 审查以下全部维度：

### 1️⃣ 代码逻辑审查
- 语法正确？ESM/CJS 兼容性处理好？
- 边界值覆盖（空数据、单值、极值）？
- 动态生成文本是否唯一（不雷同/不重复）？

### 2️⃣ 数据一致性
- 字段结构匹配？数据引用完整？
- 配置与代码约定一致？

### 3️⃣ 安全审查
- 无硬编码密钥/token？
- 无 shell 注入风险？

### 4️⃣ 隐私安全（🧪 2026-05-16 新增）
- 输出是否包含用户本地路径（如 `/Users/xxx/`）？
- 输出是否暴露用户名、公司名等个人信息？
- 知识库依赖是否降级（无知识库时不 crash）？
- 文件路径/文件名在输出中是否脱敏？

### 5️⃣ 平台兼容性（🧪 2026-05-16 新增）
- **SkillHub 可上传**：所有文件类型是否在允许范围内？
  - ❌ 禁止：`.hbs`、`.exe`、`.dll`、`.so`、`.pyc` 等非文本/可执行文件
  - ❌ 禁止：`templates/` 目录（SkillHub 不识别，建议内联到代码）
  - ❌ 禁止：`LICENSE` 文件（触发校验错误）
  - ❌ 禁止：`node_modules/`、`package-lock.json`
- **ClawHub 可发布**：
  - package.json 中 name/version 格式正确
  - SKILL.md 和 README.md 存在且内容完整
- **跨平台兼容**：路径分隔符、文件编码是否考虑跨 OS？

---

## § 推送前检查清单（Tech 发布前自检）

### 结构
- [ ] 子目录结构正确？README 在 `<skill-name>/` 下，不在根目录
- [ ] 不包含 `node_modules/`、`package-lock.json`
- [ ] 不包含 `LICENSE` 文件（SkillHub 会报错）
- [ ] 不包含 `templates/` 目录（模板内容已内联到代码）
- [ ] 所有文件类型在 SkillHub 允许范围内

### GitHub
- [ ] 推送后检查仓库文件列表
- [ ] `.gitignore` 包含 node_modules/

### 本地验证
- [ ] 本地跑一轮完整输出，检查所有动态文本（卡片/列表/挑战/功能描述）
- [ ] 检查边界值：单值预算、不同月份、空数据场景
- [ ] 输出不包含隐私信息（路径/用户名/公司名）

### 发布
- [ ] 版本号正确更新（package.json + cli.js）
- [ ] 已通过 Checker 全部 5 项审查

---

## 关键规则

1. **矛盾独立创作** - 不强制要求每技能必写文章，按需触发
2. **信息同步** - 二蛋负责在关键节点同步信息
3. **状态更新** - 技能状态变更实时更新到多维表格
4. **审核机制** - 所有产出最终由李宁审核

---

## 当前进度

| 技能 | 状态 | 文章 |
|------|------|------|
| tob-sales-proposal | ✅ 已完成 | ✅ 待审核 |
| tob-competitor-analyzer | 📝 待开始 | - |
| ... | ... | ... |
